# [技术方案] ⟨模块名⟩ — ⟨功能名⟩

| 项 | 值 |
|---|---|
| 状态 | ⟨草稿 / 评审中 / 已确认 / 开发中 / 已验证 / 已上线 / 已日落⟩ |
| 负责人 | ⟨@谁⟩ |
| 业务域 | ⟨NN_[业务地图]XX域⟩ |
| 前台路由 | ⟨`/xxx`⟩ |
| 后台 API | ⟨`/api/xxx`⟩ |
| 来源 | ⟨PRD 链接 / 评审纪要 / 决策对话 + 日期；无出处不予评审⟩ |
| 适用范围 | ⟨全项目 / NN_[业务地图]XX域 / 单模块⟩ |
| 创建 / 更新 | ⟨YYYY-MM-DD⟩ / ⟨YYYY-MM-DD⟩（更新时间即最后确认时间，超过 ⟨90⟩ 天视为待校准） |

---

## 1. 背景与目标

- **要解决什么问题**：⟨一句话，写用户视角的痛点⟩
- **不解决什么**：⟨明确边界，防止范围膨胀⟩
- **目标指标**：⟨可量化的验收指标，如「创建耗时 < 3s」⟩

### 1.1 准入四问（写不出答案，不开工）

- [ ] **问题-方案匹配**：方案确实解决第 1 节的问题，不是顺手做别的
- [ ] **范围受控**：「不解决什么」已写清；后续任何超出项 = 拒绝或另起 spec，不默认接下
- [ ] **复用优先**：现有能力已盘点（⟨列出可复用的模块/接口⟩），无重复造轮子
- [ ] **验收可逆**：做错能回滚（与第 6 节兜底方案对得上）

### 1.2 待确认问题（显式标记未知，禁止带着假设开工）

| 问题 | 等谁确认 | 影响哪一节 |
|---|---|---|
| ⟨写不准就列在这里，别编造⟩ | ⟨@谁⟩ | ⟨§N⟩ |

## 2. 用户故事

- 作为 ⟨角色⟩，我希望 ⟨动作⟩，以便 ⟨价值⟩

## 3. 方案设计

### 3.1 整体流程

⟨主流程描述 / 流程图链接⟩

### 3.2 接口契约

| 接口 | 方法 | 入参 | 出参 | 幂等 | 备注 |
|---|---|---|---|---|---|
| ⟨`/api/x`⟩ | POST | ⟨…⟩ | ⟨…⟩ | ⟨是/否⟩ | |

### 3.3 数据模型

⟨字段变更 / 新增表 / 索引，标注是否破坏性⟩

### 3.4 关键决策与取舍

⟨为什么选 A 不选 B。复杂度高的另起 ADR，链接到这里⟩
→ 见 [ADR](../../architecture/ADR/)

## 4. 影响面

- **依赖模块**：⟨谁会被影响，负责人是谁⟩
- **配置项**：⟨新增/变更的配置，是否外置到平台（外置需缝补，见 context-stitching）⟩
- **数据迁移**：⟨有/无，有则写迁移脚本与回滚方式⟩
- **兼容**：⟨是否需要灰度、双写、开关⟩

## 5. 验收标准

> 必须可测试。禁止「体验良好」「性能达标」这类不可判定的描述。
> **每条必须编号 `AC-N`**（AC-1 起递增）——编号是与 `tests.md` 的连接键，
> 测试点靠它回指验收标准。编号一经使用不要复用（删除的编号留空）。

- [ ] `AC-1` ⟨给定 ⟨条件⟩，当 ⟨操作⟩，则 ⟨可观测结果⟩⟩
- [ ] `AC-2` ⟨…⟩

> 本功能的测试点写在同目录 [`tests.md`](./tests.md)（复制 `_templates/tests.md` 新建），
> 每条 AC 至少对应一个测试点。


## 6. 风险与兜底

| 风险 | 概率 | 影响 | 兜底方案 |
|---|---|---|---|

## 7. 排期与里程碑

| 阶段 | 时间 | 交付物 |
|---|---|---|

## 8. 实施任务

> 实现层的分步清单（`T-N` 编号），提交时按覆盖范围勾选。
> 全部勾选且提交关卡的 spec 差异清单无未决项，才允许把状态标为「已验证」。

- [ ] `T-1` ⟨实施步骤⟩
- [ ] `T-2` ⟨…⟩

---

## 变更历史

| 日期 | 类型 | 说明 | 链接 |
|---|---|---|---|
| ⟨YYYY-MM-DD⟩ | feature | 初版 | — |
<!-- patch 追加示例（复制到本目录后才成立）：
| 2026-09-09 | patch | 调整了分页策略 | PATCH-001-分页策略.md |
-->
