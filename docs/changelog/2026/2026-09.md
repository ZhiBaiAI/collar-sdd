# 2026-09 变更时间线

> 真实记录由 `collar-changelog` 在 commit 时追加。
> 带「示例」标注的条目为示范数据，落地清理时删除。

### 2026-09-18 · chore · 双语 README 重构：标准开源骨架 + 配套看板 collar-board 入口

- **Spec**：— （模板自身能力）
- **影响面**：`README.md`、`README.en.md`
- **变更**：① README 按开源项目标准骨架重排：标题 + badge（License / PRs welcome）→「为什么需要它」（动机独立成节）→「它是什么」（五组成表格总览）→ 快速开始 → 目录结构 → 渐进采用 → 可视化看板 → 纪律 → 裁剪 → 贡献 & 许可。② 新增「可视化看板：collar-board」节——只读看板、两步启动、只读/不评分两条设计底线、Chrome/Edge 要求，中英双版同步。③ 补「参与贡献 & 许可」入口（此前 LICENSE / CONTRIBUTING 存在但 README 无链接）。④ 修冷启两处坑：git clone 场景需先删 `.git`；「冷启后删三节」注明保留「跟进模板更新」小节。⑤ 顺手清了一个 collar-board 链接手误（写成 collar-sdd）。
- **破坏性**：无——文档改动，不触及任何机制
- **关联**：[collar-board](https://github.com/ZhiBaiAI/collar-board)、[CONTRIBUTING.md](../../CONTRIBUTING.md)

### 2026-09-17 · fix · 示范域 tests.md 补 RENAMED 换号（AC-1 → AC-P001-4）

- **Spec**：示范域 / 核心循环（示范数据修正）
- **影响面**：`docs/specs/01_[业务地图]示范域/01_核心循环/tests.md`
- **变更**：PATCH-001 RENAMED 把 `AC-1` 换成 `AC-P001-4`，但示范 tests.md 的 TC-1 仍只回指旧号——新版 collar-board 的覆盖核对据此抓到「AC-P001-4 无测试点」。TC-1 对应列双写 `AC-1 → AC-P001-4`（收敛前 spec 仍声明 AC-1），变更记录补一行。
- **破坏性**：无——示范数据修正
- **关联**：—

### 2026-09-17 · feature · collar-sync 模板升级执行体 + VERSION 版本机制

- **Spec**：— （模板自身能力）
- **影响面**：新增 `scripts/collar-sync.sh`、`VERSION`；`scripts/collar-check.sh`（S0）、`AGENTS.md`、`README.md`/`README.en.md`、`docs/specs/README.md`
- **变更**：解决「下游项目复制模板后如何跟进上游演进」——`collar-sync.sh` 按三层所有权分层同步：①机械资产（scripts/ skills/ _templates/ VERSION）直接覆盖并同步上游删除；②骨架文档（runbook、各 README、AGENTS.md、collar.yaml）只出 diff 报告人工挑；③项目自有内容不碰。①类有本地改动时拒绝覆盖除非 `--yes`（提示把修改上游化）。新增 `VERSION` 文件承载模板版本号并纳入 S0 骨架检查；README 补「跟进模板更新」节（中英双版）。
- **破坏性**：无；S0 新增 VERSION 为必查文件（老下游 sync 时会一并获得）
- **关联**：—

### 2026-09-17 · fix · 仓库体检修复包：开源要件 + 死代码/漂移修复

- **Spec**：— （模板自身能力）
- **影响面**：新增 `LICENSE`(MIT)、`.github/workflows/collar-check.yml`、`CONTRIBUTING.md`；`scripts/collar-status.sh`、`collar-init.sh`、`collar-new.sh`、`collar-check.sh`；`AGENTS.md`、patch 模板、`docs/specs/README.md`、`skills/collar-changelog`
- **变更**：① 补齐开源要件——MIT LICENSE、结构门禁 CI（与本地 hooks 同一执行体，未装 hooks 的提交也被拦）、CONTRIBUTING 快速指引。② 修 `collar-status` 在途 sunset 死代码：原判据全文搜「归档」对每个 sunset 恒真导致永不列出，改为读归档字段（真实日期且不含 ⟨⟩）/「已归档」字样。③ S5 硬性要求 RENAMED TO 必须 `AC-PNNN-N` 形——堵死「普通编号 TO 触发 S3 悬空引用直到收敛」的自相矛盾路径。④ `collar-init` Next 行补上 `collar-hooks.sh` 步骤（冷启漂移修复）。⑤ `collar-new.sh` 支持 `proposal` 类型（模板与文档本有通道、脚本缺失）。⑥ AGENTS.md 瘦身 3 行至 ~110（合并质量门禁行与验收/测试行，余量回到 10 行）。⑦ changelog 标签表补注：commit-msg 放行但未列出的类型归 chore。
- **破坏性**：无——新增文件为主；S5 新增 RENAMED TO 编号形制约束（旧 patch 无 RENAMED 不受影响）
- **关联**：— 

### 2026-09-17 · feature · Delta 四段化 + S5 一致性加固（借鉴 OpenSpec validator）

- **Spec**：— （模板自身能力）
- **影响面**：`scripts/collar-check.sh`、`scripts/collar-converge.sh`、`docs/specs/_templates/patch.md`、`docs/specs/README.md`、`collar.yaml`、`skills/collar-specs/SKILL.md`、示范 patch
- **变更**：① patch §⑥ 新增 **`### RENAMED`** 段（`- FROM: `AC-旧号`` 紧跟 `- TO: `AC-新号`` 成对）——改编号不改正文，收敛时机械换号；`collar-converge.sh` 已支持。② S5 从「查引用存在性」扩展为 delta 一致性全查：小节内重复 / 跨小节同编号 / FROM-TO 不配对 / RENAMED TO 已存在 / 段外 AC 孤儿行 / 小节标题疑似拼错——此前这类错误会被 converge 静默吞掉。③ 全部 FAIL 信息统一带「修法：」前缀的可操作提示。④ `collar-specs` 模式A 新增 Step 0「需求澄清」守则：先查证再提问、一次一题、分层记录（沉默≠接受）、适可而止。
- **破坏性**：旧格式 patch（无 Delta 小节）此前 converge 已拒绝，无新增破坏；有 delta 的老 patch 若存在跨段冲突会被新 S5 拦出
- **关联**：— 

### 2026-09-17 · feature · 六个 collar-* 技能结构升级（借鉴 OpenSpec 工作流模板）

- **Spec**：— （模板自身能力）
- **影响面**：`skills/` 全部六个 SKILL.md + `skills/README.md`
- **变更**：① collar-specs 模式B 从「自由格式差异清单」升级为**输出契约**——五维记分卡 + CRITICAL/WARNING/SUGGESTION 三级分级（语义冲突=CRITICAL，超集/测试漂移=WARNING），每条必附 `文件:行` 与可操作建议，拿不准往轻了报，全绿也显式报「全部通过」。② 六技能统一加 `## 护栏` 节——散在叙述文里的禁令提炼成可扫读的硬否定句（changelog/runbook 新增，architecture 新建，vendor/wiki 由「红线」改名扩容，specs 由「禁止事项」升级）。③ 有参数的技能 frontmatter 加 `argument-hint`；各技能主流程末尾补 `Next:` 衔接行。④ `skills/README.md` 新增「结构约定」固化以上格式。
- **破坏性**：无——内容增量与原节改名（红线→护栏），无行为变化
- **关联**：— 

### 2026-09-17 · fix · collar-converge AC 匹配收紧为行首判定

- **Spec**：— （模板自身能力）
- **影响面**：`scripts/collar-converge.sh`
- **变更**：spec.md 的 AC 匹配从「行内含反引号 `AC-N`」收紧为「行首即 `- [ ] \`AC-N\``」，避免命中正文/备注里的编号引用并防止 ADDED 项插错位置。
- **破坏性**：无
- **关联**：— 

### 2026-09-17 · feature · git hooks 随模板自带（提交关卡从协议执行升级为技术拦截）

- **Spec**：— （模板自身能力）
- **影响面**：新增 `scripts/hooks/`（4 个钩子）与 `scripts/collar-hooks.sh`（装配器）；`docs/runbook/commit-gate.md`、双语 `README`、`AGENTS.md`、`docs/specs/README.md`
- **变更**：① `scripts/hooks/` 预置 4 个 POSIX sh 钩子——`pre-commit`（collar-check + 逐条执行 collar.yaml `kind:quality` 的 cmd，⟨占位符⟩ 自动跳过）、`commit-msg`（首行「<类型>(<范围>): <说明>」校验，WIP 豁免）、`post-commit`（触及 specs/src/scripts 时提醒补 changelog/runbook）、`pre-push`（结构门禁复跑 + ⑤⑥⑦ 协议项自查）。② `collar-hooks.sh` 用 `core.hooksPath` 装配而非拷贝进 `.git/hooks`——hook 内容版本化、全团队共享同一份，clone 后跑一次；支持 `--check` / `--remove`。③ commit-gate.md「装配方式」改为以自带 hooks 为主路径，husky/CI 列为等价挂接点。
- **破坏性**：无（不装 hooks 行为不变，装配是显式一步）
- **关联**：[commit-gate.md](../../runbook/commit-gate.md)、[scripts/collar-hooks.sh](../../../scripts/collar-hooks.sh)

### 2026-09-17 · feature · 变更过程机械化（借鉴 OpenSpec：Delta 验收标准 + 操作脚本 + authoring 规则一处真相）

- **Spec**：— （模板自身能力）
- **影响面**：`docs/specs/_templates/`（patch / feature）、两个示范域的 PATCH 与 tests.md、`scripts/`（新增 4 个）、`collar.yaml`、`docs/specs/README.md`、`docs/runbook/commit-checklist.md`、`skills/collar-specs`、`AGENTS.md`、`README.md`、`docs/vendor/SOURCES.md`
- **变更**：① patch §⑥ 从自由验收清单改为 **Delta 三段**（`ADDED` 用 `AC-P⟨NNN⟩-N` / `MODIFIED` 替代主文档 `AC-N` / `REMOVED` 作废），使收敛可机械合并；结构门禁 S5 同步升级为校验 delta 引用编号存在。② feature §8 / patch §⑦ 新增**实施任务**（`T-N` / `T-PNNN-N`），feature/patch 状态机补「已验证」节点（任务全勾 + 差异清单无未决项才许标）。③ 新增 4 个 POSIX sh 操作脚本：`collar-new.sh`（建变更自动编号）、`collar-status.sh`（在途导航 + `--specs` 机器可读清单 + `--json`）、`collar-converge.sh`（patch Delta 机械合并进 spec.md 并标已收敛）、`collar-init.sh`（冷启清理示范内容 + 占位符清单 + 跑门禁）；所有脚本输出末尾带 `Next:` 导航，`collar-check.sh` 同步加。④ `collar.yaml` 新增 `authoring` 节：项目 context + 每类产物写作规则集中声明，Skill 生成文档时读取。⑤ OpenSpec 登记进 vendor 参考资产（机制借鉴来源，不落代码）。
- **破坏性**：**有（对既有 patch 文件）** —— §⑥ 小节格式变更（`### ADDED/MODIFIED/REMOVED`），旧格式 patch 不触发门禁失败但无法被 `collar-converge.sh` 解析，需手工迁移；patch 状态枚举从「草稿/已合并/已废弃」改为「草稿/实施中/已验证/已收敛/已废弃」
- **关联**：[docs/specs/README.md](../../specs/README.md)、[vendor/SOURCES.md](../../vendor/SOURCES.md)（OpenSpec 条目）

### 2026-09-15 · feature · 结构门禁上线，中心索引改为可推导信息（知识库治理机制升级）

- **Spec**：— （模板自身能力）
- **影响面**：新增 `scripts/collar-check.sh` 与 `docs/architecture/principles/`；`AGENTS.md`、`collar.yaml`、`docs/runbook/`（commit-gate / commit-checklist / testing / conventions）、`docs/specs/README.md`、`docs/architecture/README.md`、`docs/changelog/README.md`、`skills/collar-specs`、`skills/collar-runbook`、双语 `README`
- **变更**：① 新增结构门禁 `collar-check.sh`（7 项：入口行数 / tests.md 伴生 / AC 双向对齐 / ADR 编号唯一 / patch 双向指针 / changelog 联动 / 会话指代词），语言无关、复制即生效；`collar.yaml` 门禁据此拆为 structure（真实命令）与 quality（占位待装配）两类。② 删除三个并行冲突热点：AGENTS.md 当前状态表、testing.md 覆盖索引、认领表的路由/API 两列——可推导信息不再手抄。③ architecture 模块补齐「当下事实层」：新建 principles/（模板 + P-001 外置即缝补 + 索引），`collar-architecture` Skill 同步补 Step 3.5（维护原则），结构视图立「只写现在时」纪律，ADR 状态机补 `rejected` 态与「未来决策价值」归档标准。④ 新增约定 C-006，月度回顾扩「约定退役 / ADR 归档」两问。⑤ 复查修复门禁三处缺陷：S5 报错时因全角标点紧邻变量而崩溃（已全量改为 `${}` 包裹）、S6 因模板自带示范条目恒真（改为要求 changelog 同次提交）、S6 因 git 默认转义非 ASCII 路径而静默失效（加 `core.quotePath=false`）。⑥ 二轮复查加固三项：S5 对不符合 `PATCH-NNN-简述` 命名的文件改报明确的格式错误（原误报 `PATCH-PATCH-NNN`）；S2/S3/S5 的 find 循环改 heredoc 逐行读，修复目录名含空格时的分词误报；新增 S0 骨架守卫——核心文件/目录被误删时显式失败（原 grep 静默吞错、门禁照常放行）
- **破坏性**：**有（对模板使用方）** —— 认领表列数变少、testing.md 节号重排（§6→§5、§7→§6）、commit-checklist 从 6 区块变 7 区块；已按新结构同步所有交叉引用
- **关联**：[commit-gate.md](../../runbook/commit-gate.md)、[conventions.md](../../runbook/conventions.md)、[scripts/collar-check.sh](../../../scripts/collar-check.sh)

### 2026-09-11 · feature · 新增功能点测试文档机制（tests.md + AC 编号连接键）

- **Spec**：— （模板自身能力，示范见 [示例域/01_示例功能](../../specs/00_%5B业务地图%5D示例域/01_示例功能/tests.md)）
- **影响面**：`docs/specs/`（新增伴生文档 `tests.md` 与模板）、`docs/runbook/testing.md`（新增测试知识总入口）、`skills/collar-specs`、`skills/collar-runbook`、`AGENTS.md`、`docs/runbook/commit-checklist.md`
- **变更**：每个功能点新增伴生测试文档 `tests.md`，验收标准编号为 `AC-N`（patch 为 `AC-P⟨NNN⟩-N`），测试点回指 AC，使「验收标准可测试」可机械核对；跨度知识（分层口径 / 运行时机 / 冒烟清单）集中于 `docs/runbook/testing.md`
- **破坏性**：无
- **关联**：[docs/runbook/testing.md](../../runbook/testing.md)

### 2026-09-09 · patch · 分页策略调整为游标分页 · ⟨@谁⟩〔示例〕

- **Spec**：[示例域/01_示例功能](../../specs/00_%5B业务地图%5D示例域/01_示例功能/spec.md)
- **影响面**：`/api/demo` 接口签名、调用方
- **变更**：偏移量分页（`page`）改为游标分页（`cursor`），`pageSize` 上限 100
- **破坏性**：**有** —— `page` 参数废弃；灰度期内并存，传 `page` 打告警日志
- **关联**：[PATCH-001](../../specs/00_%5B业务地图%5D示例域/01_示例功能/PATCH-001-分页策略.md)

### 2026-09-01 · feature · 示例功能上线 · ⟨@谁⟩〔示例〕

- **Spec**：[示例域/01_示例功能](../../specs/00_%5B业务地图%5D示例域/01_示例功能/spec.md)
- **影响面**：新增 `/demo` 路由与 `/api/demo` 接口
- **变更**：提供列表查询能力
- **破坏性**：无
- **关联**：—
